## Frontend (required)
# Deployed Memo contract address on Sepolia (or your target chain if you also update Wagmi config).
NEXT_PUBLIC_MEMO_CONTRACT_ADDRESS=

## Deployment (required for Sepolia deploy)
# RPC endpoint for Sepolia. Example: https://eth-sepolia.g.alchemy.com/v2/XXXX
SEPOLIA_RPC_URL=

# Deployer private key (DO NOT COMMIT `.env.local` / `.env`).
# Note: private key is 64 hex chars; an address looks like 0x + 40 hex chars.
PRIVATE_KEY=

